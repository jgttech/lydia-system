import { createUseStyles } from "react-jss";

export default createUseStyles({
    nomenclature: {
        fontStyle: "italic",
        color: "rgb(180, 180, 180)"
    },
    id: {
        fontWeight: "bold"
    },
    input: {
        margin: "2rem 0"
    }
});